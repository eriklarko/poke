import 'package:flutter/material.dart';

class PokeHeader extends StatelessWidget {
  final String text;

  const PokeHeader(this.text, {super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: style lol
    return Text(text);
  }
}

// Regular text
class PokeText extends StatelessWidget {
  final String text;

  const PokeText(this.text, {super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: style lol
    return Text(text);
  }
}

class PokeFinePrint extends StatelessWidget {
  final String text;

  const PokeFinePrint(this.text, {super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: style lol
    return Text(text);
  }
}
